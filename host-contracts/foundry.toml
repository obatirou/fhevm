[profile.default]
src = 'contracts'
out = 'out'
libs = ['node_modules', 'lib', "dependencies"]
test = 'test'
cache_path  = 'cache_forge'
solc = '0.8.24'

[fuzz]
# The number of fuzz runs for fuzz tests
runs = 2560

[dependencies]
forge-std = "1.11.0"
